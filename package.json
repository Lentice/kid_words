{
  "name": "kids-english-flashcards",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build && node -e \"require('fs').copyFileSync('dist/index.html', 'dist/404.html')\"",
    "preview": "vite preview --strictPort --port 5173",
    "deploy:imp": "npm run build && npx gh-pages -d dist -r https://github.com/Lentice/kid_words.git -b gh-pages && git checkout main",
    "deploy": "npm run deploy:imp || git checkout main"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.9.6",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "cpy-cli": "^6.0.0",
    "gh-pages": "^6.2.0",
    "rimraf": "^6.1.0",
    "vite": "^5.4.8"
  }
}
